# Get all published products
# This query is part of the allow-list for production mode

query getProducts {
  products(
    where: { is_published: { eq: true } }
    order_by: { created_at: desc }
    limit: 20
  ) {
    id
    name
    description
    price
    stock
    created_at
  }
}
